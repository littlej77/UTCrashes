@{
    ViewData["Title"] = "Home Page";
}

@model List<Crash>

<h1> Crashes </h1>
@*<ul>

    @foreach (var c in Model)
    {
        <li>@c.CRASH_ID -- @c.COUNTY.COUNTY_NAME</li>
    }
</ul>*@

@foreach (var c in Model)
{
    <partial name="CrashSummary" model="c" />
}